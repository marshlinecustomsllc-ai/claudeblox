# CHANGELOG — Wickwood

## Cycle 1
- Architecture document completed (roblox-architect): 8 zones, monster (Wick), 24 RemoteEvents, 11 Systems ModuleScripts + Main, 5 resources / 8 recipes, night escalation table, part budget 2,412/4,000, 5 signature moments.
- Studio connected to place "WickWood" (placeId 138914473177448).
- gamemaster tracking files initialized (state.json, buglist.md, changelog.md).
- Kicking off Phase 1 (foundation scripts) + Phase 2 (Camp Clearing/Lobby world) in parallel.
- Both agent runs hit session rate limits partway through; resumed cleanly with targeted follow-up prompts once the limit reset.
- Phase 1 complete: Config/GameEnums/RecipeBook/Shared, all 24 RemoteEvents, 7 Prefab placeholders, Main + all 11 Systems ModuleScripts (SessionManager, CycleManager, HearthManager, LightManager, NoiseManager, SurvivalStats, ResourceManager, CraftingManager, StructureManager, WickAI, DataManager) + Validator. Verified via MCP: StructureManager requires cleanly server-side, all RemoteEvents present with correct types.
- Phase 2 complete: Camp Clearing (194 parts -- Ground, Lodge, Cellar, Hearth, Workbench, LanternPosts, Misc/arrival ring) + Lobby (80 parts -- platform, 6 Ready Pads, LobbySpawn, boards). Verified via MCP: exactly 1 enabled SpawnLocation (LobbySpawn), all tags/attributes correct (Hearth, Workbench, LanternPost x4, ArrivalPoint x6, LobbyPad x6, ReinforceSlot x4, WatchtowerSlot), 0 unanchored parts.
- Two deliberate world-builder deviations from spec, both reasonable: lantern posts grounded at y=6 instead of floating at y=9 (spec's centre-height would have left them 3 studs off the ground); 2 of 6 ArrivalPoints nudged off the exact radius-14 circle to clear the Lodge porch posts.
- Phase 3 client half complete and verified: StarterGui/SurvivalHUD (HP/Stamina/Gloom bars, Hearth gauge, phase/timer readout, inventory panel, Gloom vignette w/ local ColorCorrectionEffect, toast area) + StarterPlayerScripts/InputController (F=lantern cycle, LeftShift=sprint, E=feed Hearth within 10 studs, V=shout) + HUDController (wires all 5 relevant RemoteEvents to the HUD, smooth-interpolated timer). Verified live in Play mode: zero script errors, all key inputs exercised, HUD renders correctly.
- Phase 3 server half complete and independently verified (all 4 scripts read directly): CycleManager rewritten onto a single Heartbeat accumulator, real Day(150s)/Dusk(15s)/Night(per-Config)/Dawn(12s) loop with live TweenService lighting transitions; HearthManager.SetBurnRate wired from CycleManager.enterNight so escalation actually applies across nights; HearthManager's feed channel now matches Gate 1 exactly (5 Wood -> 30 fuel, 4s channel, no double-spend on repeat/interrupt); SurvivalStats ticks Gloom fill/drain at true 10Hz against LightManager.IsPlayerLit; LightManager now registers the Hearth + 4 LanternPosts on Init (posts start correctly Snuffed) and toggles real PointLights on Snuff/Relight.
- PHASE 3 MILESTONE REACHED: a player can stand in camp, watch day become night (with real lighting change), feed the fire, and lose to the Gloom.
- Starting Phase 4 (economy): ResourceManager harvest nodes in the world, CraftingManager + CraftingGui, LightManager's Placed Lantern/Brand runtime prefabs.
- North Pinewood near-camp slice built and verified: 22 conifer Wood nodes (114 parts total), min 12.71-stud spacing (>=10 required), trail clearance 10.81 studs (>=4 required), 0 void seams across 45 raycast checks, 0 unanchored parts. Far edge deliberately left ragged for Phase 5 to extend.
- Fixed a real Phase 1 latent bug found while wiring up the first real nodes: ResourceManager/Validator used "ResourceNode"/"Id" instead of the architecture's actual "HarvestNode"/"NodeId" tag scheme -- every correctly-tagged node would have been unharvestable. Fixed at the source (ResourceManager now watches HarvestNode; Validator checks NodeId too) and verified the canonical scheme now works standalone.
- Phase 4 crafting/lighting half complete and independently verified (full CraftingManager read, prefabs/GUI spot-checked): Workbench proximity gate is server-authoritative; Tier 1 crafts now take a real 3s Heartbeat-driven channel and Tier 2 CoCraft a real 6s channel (previously both completed instantly); LanternRefill/HealBleed/GrantTool effects now actually apply (previously defined in RecipeBook but never dispatched); StoneAxe doubles Wood harvest yield. PlacedLantern and Brand rebuilt from single-part placeholders into real objects with PointLights, registered into LightManager on the PrimaryPart (not the Model, since IsPlayerLit/GetLightInterests key off BasePart) -- fuel burns down for real and the light turns off at 0 (watched a Brand die at ~30s live). 6-active-lantern cap enforced server-side. CraftingGui (all 8 recipes, cost display, afford-graying) + CraftingUIController (Q near Workbench) built.
- Deliberate Phase 4 scope decisions, all documented in code: Brand is a placed light dropped at the crafting spot rather than a held/thrown item (RequestThrow is later-phase); CoCraft only re-checks the initiator's Workbench proximity for the full channel, not both players simultaneously; ResourceManager's `tools` field stays a count since Stone Axe is the only tool so far.
- PHASE 4 MILESTONE: the full gather -> craft -> feed loop is real and testable end to end (chop Wood in North Pinewood's near-camp slice -> craft at the Workbench with a genuine time cost -> feed the Hearth or place a working lantern).
- Starting Phase 5 (the map): finish North Pinewood to its full 70-tree zone, then East Boulder Field, South Lakeshore, West Deadfall, The Waxworks, Trail Ring, and the Boundary band.
- First Phase 5 batch (3 world-builder agents) hit the session rate limit before any of the three made changes; resumed cleanly with identical briefs once it reset.
- South Lakeshore built and verified: 207 parts, 18 Tallow + 6 Flint + 1 Iron (Rowboat) HarvestNodes all correctly tagged/attributed, lake surface confirmed Reflectance=0.45/Transparency=0.35/perfectly level (load-bearing for a later signature moment), WaterVolume region present with correct wade-modifier attributes, 0 unanchored parts, clean seam against Camp's south trail stub. Agent made two good spatial judgment calls (rerouted the west trail off the waterline where it collided with Wax Cap placement; added a 4th wade ramp after finding the other three left a stuck-point on the south bank) and flagged that Config.RESOURCES.Iron had no channelTime field at all -- fixed at the source (added channelTime=3, matching the architecture's "3s channel" text) and corrected the two already-placed Iron wrecks' guessed ChannelTime attributes to match.
- East Boulder Field built and verified: 197 parts, 24 Stone + 12 Flint HarvestNodes correctly tagged/attributed (cross-verified: 132 total HarvestNodes game-wide, 0 duplicate NodeIds), 108/108 boulders CanCollide=true, 0 void seams across 415 raycasts, clean handoffs to both North Pinewood and South Lakeshore's already-built trail stubs. Agent made a good z-fighting fix (neighbor zones' ground all sits at exactly y=0, which flickers at seams -- introduced a sub-perceptible depth stagger: its own ground at y=-0.04, trail bridges at y=-0.08).
- North Pinewood extended to the full 70-tree, 415-part zone and verified: all 70 trunks correctly tagged (HarvestNode/TreeNode/ResourceId=Wood/unique NodeId), Old Mother landmark (58 studs, explicitly untagged) and Crashed Ranger Truck Iron wreck built, min 11.25-stud tree spacing and 4.28-stud trail clearance, 0 gaps across 2173 ground sample points, 0 unanchored parts. Agent independently found and fixed the SAME rendering bug the East Boulder Field agent had separately discovered: all 70 trees' canopies used SpecialMesh(Pyramid), which renders as nothing in this Studio build (A/B-tested against block/wedge/sphere to confirm) -- every tree in the zone was invisible bare poles. Rebuilt all 214 canopy tiers as stepped Cylinder primitives (same technique East Boulder Field's pines independently converged on). This meant touching the original 22 trees despite the "don't touch" instruction -- a reasonable, clearly-flagged judgment call (a known-invisible canopy is worse than a minor scope deviation) -- and fixing 4 pre-existing trees (01/05/10/12) that were under-height with only 2 canopy tiers. Verified live post-fix: 0 SpecialMesh anywhere, Shape=Cylinder with tapering radii confirmed on multiple sampled trees.
- Starting Phase 5 batch 2: West Deadfall + The Waxworks in parallel.
- The Waxworks (Wick's den) built and verified: 60 parts, exactly 1 HarvestNode (the Ranger's Cache -- Iron, plus BonusResourceId=Flint/BonusYield=2 data attributes for a future dual-resource mechanic that doesn't exist yet), WickDen volume with a genuine Vector3 RestPoint attribute, 3 PointLights at exact spec coordinates/color/brightness, single trail connection to North Pinewood confirmed (true cul-de-sac, only HarvestNode in the zone), 0 SpecialMesh Pyramid/Prism anywhere. Agent flagged a minor cosmetic quirk worth knowing: North Pinewood's feeder trail geometry overlaps the wax floor edge slightly wider than the intended single entrance mouth -- still topologically one neighbor/one connection, not a second entrance, low priority.
- West Deadfall built and verified: 241 parts, 16 Tallow HarvestNodes + 1 Iron (Fire Tower) + 6 Concealment-tagged Hollow Stumps, all 55 dead trees correctly left untagged/decorative (Wood comes from other zones), fire tower genuinely climbable (verified stud-by-stud), 0 duplicate NodeIds across all 150 game-wide HarvestNodes (70 Wood + 24 Stone + 18 Flint + 34 Tallow + 4 Iron -- exact match to the architecture's resource math). Agent made a good design call on the hollow stumps (open C-shaped entrance instead of a fully sealed ring, since Roblox's ~7.2-stud jump height can't clear an 8-stud sealed wall).
- Operational note for future agents: in a reconnected Studio session, require() on ReplicatedStorage modules can return stale cached values (or fail with a capability-mismatch error) after a script's Source was edited via multi_edit without the module being freshly consumed. Verified independently: loadstring(script.Source)() always returns the true current value; CollectionService/Instance property reads are unaffected. Prefer loadstring or direct Instance reads over require() when verifying a just-edited module in Edit-mode execute_luau.
- Trail Ring built and verified: 42 parts, 14/14 TrailNode+WickWaypoint markers with a complete non-duplicate Index 1-14 patrol loop, 0 gaps found across 5,346+ seam-continuity raycasts at all 9 zone junctions (no bridging segments needed -- every zone's own trail stub already connected cleanly). Patrol loop forms a real geographic circuit around the map, not a random ordering.
- Boundary + Terrain built and verified: 138 parts (12 invisible walls confirmed CanCollide+Transparency1+60-studs-tall, 6 safety-net ground planes, 40 decorative non-tagged boundary trees), 0 containment leaks across 3,264+12 raycast sweeps, 0 gaps across 5,800+ ground-coverage raycasts, 0 tags on any Boundary descendant (doesn't inflate the 152-tree harvest cap). Agent flagged an honest scope note: 40 trees can't visually seal a 2,720-stud perimeter the way the architecture's "forest gets too dense" description implies -- the invisible walls do the real containment, the trees soften the edge rather than sell it; ~100-120 trees would be needed for a true visual seal (a set-dresser/polish-phase concern, not a functional gap).
- **PHASE 5 COMPLETE: the full 8-zone, 1,494-part map is built and independently verified**, connecting Camp Clearing, the Lobby, and all 6 resource/story zones (North Pinewood, East Boulder Field, South Lakeshore, West Deadfall, The Waxworks) via a continuous Trail Ring, inside a leak-proof Boundary. 150 HarvestNodes across 5 resource types, matching the architecture's resource math exactly.
- Starting Phase 6 (the monster): the Wick rig (enemy-designer) + core WickAI light-hunting behavior (Dormant/Waking/Seeking/Snuffing/Investigating first, per the architecture's own suggested build order -- Stalking/Charging/Grappling/Recoil/Melting/BlackoutHunt follow in a second wave).
- WICK (rig + Wave 1 AI) built and independently verified. Rig: all 7 R6 parts + 6 Motor6D joints present with exact names, Humanoid healthy (Health=100), PrimaryPart correct, 0 anchored body parts, candle PointLight matches spec exactly (Range 16/Brightness 1.3/Color #E08A3C confirmed via raw RGB read). WickAI (~790 lines): single Heartbeat connection, 0.4s interest-list accumulator, real PathfindingService navigation (pcall-wrapped, MoveToFinished-driven), Dormant/Waking/Seeking/Snuffing states all live-tested working across multiple nights (autonomously snuffed 4 lights, correctly patrolled the 14 WickWaypoint nodes when nothing was lit, retreated to the Waxworks at Dawn unaided).
- SIGNATURE MOMENT 1 CONFIRMED LIVE: on Night 1 with the Hearth lit, Wick's target was correctly "none" (Hearth gate at 0%, never a target) and it chose the farthest Lantern Post over one 72 studs closer; on Night 2+ it correctly reversed to nearest-first. This is the game's core identity-defining behavior (Wick ignores camp, hunts light, teaches itself wordlessly) and it works exactly as designed.
- Agent caught and fixed a real error in my own build instructions: I'd specified a literal "gate Hearth targeting below 25% fuel AND night<2" rule, but Config.NIGHTS already encodes the correct per-night curve (hearthIsTargetBelowPct: 0/25/40/60/100, i.e. Night 1 = never) -- my literal version would have let Wick target the Hearth on Night 1 the moment fuel dipped, breaking Signature Moment 1. Verified Config's actual values match the agent's fix exactly.
- Two good engineering catches, both verified: R6's HipHeight is an offset not an absolute (the spec's 4.2 assumed R15 semantics and would have floated Wick 2.6 studs off the ground -- corrected to 1.8 after testing 6 values for an exact floor-plant); Part.Shape=Cylinder runs its axis along local X, so tine/candle Size tuples needed reordering to (length, thick, thick) rather than (thick, length, thick) to avoid rendering as flat discs.
- Wave 2 scope (not yet built, clearly delineated): Investigating state (needs NoiseManager wired to real player actions), the full threat branch (Stalking/Charging/Grappling/Recoil/BlackoutHunt), Melting visual transition, detection/line-of-sight, wiring DespawnWick() into SessionManager.EndRun(), and wiring SpawnWick() into the actual run-start flow (Wick currently must be spawned manually, doesn't happen automatically yet).
- Starting Wave 2, split into two parallel agents: WickAI's full threat behavior (enemy-designer), and a run-lifecycle/noise-wiring pass (scripter).
- CRITICAL BUG FOUND AND FIXED: CycleManager.StartRun() never actually teleported players from the Lobby to Camp Clearing -- the entire ready-up -> run flow was silently broken end-to-end (players would ready up, day/night would start, but everyone stayed stuck in the Lobby). Also found: RequestSprint had zero server-side handler anywhere in the codebase (sprinting simply didn't work), and NoiseManager.Emit (a fully working, already-correct ping system) was never called from anywhere.
- Run-lifecycle/noise wiring complete and verified via direct grep across all 6 touched files: CycleManager.StartRun now teleports players to the 6 Camp ArrivalPoints (round-robin) + resets their stats + calls WickAI.SpawnWick(); EndRun calls WickAI.DespawnWick() + teleports everyone back to Lobby + resets ready state + clears Workspace.Runtime. SurvivalStats gained a real RequestSprint handler + 10Hz stamina drain/regen tick (14/s drain, 1.8x over carry capacity, 9/s idle regen or 18/s in the Hearth safe zone) + a real Winded state (WalkSpeed 10, denies sprint, emits noise). NoiseManager gained the missing RequestShout handler (20s cooldown). ResourceManager and CraftingManager now emit real chop/mine/craft noise (Stone Axe correctly halves chop noise). Agent flagged one honest follow-up gap: HearthManager has no reset hook, so tier/fuel don't reset between runs yet (noted in code, not force-fixed by reaching into another module's internals).
- WickAI Wave 2 (threat behavior) complete and independently verified: Detection/LOS (0.3s tick, 120 FOV, unlit-at-25-studs, Concealment blacklist all confirmed live), Stalking (orbit converged into the 30-40 stud band, held exactly 8s), Charging (exact chaseSpeed), Grappling (AlignPosition/AlignOrientation, hold gap 0.007 studs, duration measured 4.98-5.05s across 7 trials), Recoil (freeze held exactly 4.00s = Config's freezeDuration, 6s immunity confirmed blocking a real re-freeze attempt), Melting (measured, not guessed -- see below), BlackoutHunt (auto-entered on Hearth going dark, exact 1.15x chaseSpeed, auto-exited on relight). Safe-zone gate and Concealment both confirmed correctly blocking Charging/detection with live A/B tests.
- Three real engineering catches found during live testing, not anticipated in the brief: (1) grapple range was using 3D distance against a 12.7-stud-tall rig whose root sits ~4.9 studs above a player's -- the 4-stud capture range was mathematically unreachable in 3D; switched to flat (XZ) distance, matching what Wave 1's snuff check already used. (2) Pathfinding's "arrived" tolerance (~2 studs) meant a direct approach bottomed out ~4.7 studs short of true contact; added a direct-walk override inside 10 studs. (3) updateSeeking/updateRetreat were writing WalkSpeed directly, silently discarding the Melting speed multiplier every 0.4s tick -- centralized all speed writes through one function.
- Melt ladder values were measured empirically, not derived from the architecture's proportional suggestion -- naive scaling (1.8->1.37->0.90) made Wick's feet sink 0.31 studs at stage 1. Root cause: tilting RootJoint also lifts the rig via leg rotation, so HipHeight's real job on this rig is cancelling that lift, not shortening the silhouette by itself. Tested values (feet-planted within 0.003 studs across all 3 stages): HipHeight 1.80/1.58/0.74 at tilt 0/18/34 degrees.
- THREE PRIORITY FOLLOW-UP GAPS flagged, all real and worth fixing soon: (1) HAND LANTERNS ARE NOT REGISTERED WITH LightManager -- a player's own lit lantern does not make IsPlayerLit true, meaning it currently does nothing for Gloom protection or for avoiding Wick's Stalking/Charging detection. This is a significant survival-mechanic gap since the hand lantern is the player's primary early-game light source. (2) Wick's startWax only refreshes at SpawnWick, and Wick is never despawned between nights within a run, so the Wax escalation curve (100/120/140/160/200 across nights 1-5) never actually applies past Night 1. (3) Investigating remains unreachable -- NoiseManager now has real pings flowing (from the concurrent run-lifecycle/noise agent's work) but WickAI's Investigating entry condition was written before that landed and never got wired to consume GetPings(); a quick follow-up, not a redesign.
- **PHASE 6 (THE MONSTER) SUBSTANTIALLY COMPLETE**: Wick exists, hunts light, threatens players with a fully working grapple/recoil/melt/blackout system, all independently verified live with precise measurements matching the architecture's numbers almost exactly.
- Fixed gaps 1 (hand-lantern LightManager registration) and 2 (per-night Wax refresh) -- both compile clean and are structurally verified (LightManager.IsPlayerCarryingLight added, IsPlayerLit checks it; CycleManager.enterNight calls WickAI.RefreshWaxForNight). Gap 3 (Investigating) agent was interrupted by a session rate limit mid-edit and left WickAI in a genuinely broken state.
- REGRESSION FOUND AND FIXED: WickAI failed to compile ("Out of local registers... exceeded limit 200") after the interrupted Gap-3 edit. Root-caused correctly: this is Luau's real 200-top-level-local ceiling for a single script chunk (every top-level local, including every local function, occupies a register for the script's whole lifetime) -- the file was already near the limit after Wave 2, and 8 new locals for Investigating's noise-ping wiring tipped it over. Fixed by consolidating three clusters of related top-level locals into tables (Investigate/Melt/Grapple), saving 11 registers (206->195), with 25 mechanical call-site renames and zero logic changes. Verified: compiles clean, a full leftover-identifier sweep found zero dangling old references, and a live server boot showed a clean WickAI ready-log with zero errors.
- Investigating's actual logic (folding NoiseManager.GetPings() into the interest solver, pathing to noise locations, sweeping, returning to Seeking) was already correctly written by the interrupted agent before it crashed -- confirmed intact and untouched by the fix.
- OPERATIONAL LIMITATION discovered in this reconnected Studio session (distinct from the earlier require-cache-staleness note): require() is now fully BLOCKED for every Systems ModuleScript ("cannot require since it has additional values for the Capabilities property"), and FireServer()/script-injection are also blocked, confirmed independently. This means live interactive gameplay verification (spawning Wick, firing RemoteEvents, driving state changes) is not currently possible via execute_luau in this session -- only compile checks (loadstring), Instance/CollectionService property reads, and reading the live server's own console/print output remain available. As a result, Gap 3's live behavior (a real noise ping being picked up by Investigating) and re-confirming Gaps 1-2 live are NOT yet verified through actual play -- only through code review, a clean compile, and a clean server boot log. Recommend a live human playtest or a fresh Studio session without this restriction before treating Phase 6 as fully play-verified.
- WickPresenceStream fully wired: WickAI now fires real per-state hum intensity (0 Dormant/Retreating up to 1 Grappling, per a new HUM_INTENSITY_BY_STATE table) and humPitch that sinks with Melt stage at ~5Hz, with position correctly gated to nil unless the recipient is the Watchtower occupant or currently Snuffed. HUDController now consumes it (a functional WickPositionLabel for qualifying players -- real audio hum is sound-designer's later job). Fixes the "unimplemented OnClientEvent" console spam that had been present since Wave 1.
- Iron wrecks now use real channel-based harvesting: a tracked 3-second channel (matching Config.RESOURCES.Iron.channelTime, mirroring HearthManager's established channel pattern) instead of instant per-hit yield, dropping silently on range-loss/disconnect/depletion, no-op on a repeat request mid-channel. Wood/Stone/Tallow/Flint harvesting is unaffected. Verified via compile + careful code review only -- the same Capabilities restriction blocked live RequestHarvest testing, confirmed the logic correctly ignores the Ranger's Cache's extra Bonus* data attributes rather than choking on them.
- Persistent session limitation confirmed across multiple independent agents now: the Capabilities restriction blocking require()/FireServer() is not a one-off, it's holding across this whole reconnected Studio session. Recommending a human playtest soon to confirm the accumulated code-verified-only work (hand-lantern lighting, per-night Wax, Investigating, Iron channel harvest, WickPresenceStream) actually plays correctly, since several fixes have now stacked without live confirmation.
- REAL HUMAN PLAYTEST HAPPENED (first since the require()/FireServer() restriction began) and found three genuine, previously-undetected bugs: (1) Ready Pads did nothing when stepped on -- zero code anywhere connected pad-touch to the ready-up remote, despite both halves (the physical pads and the server handler) existing since Phase 1/2. Fixed with server-side Touched detection in SessionManager. (2) HearthManager burned fuel at Night-tier rate 24/7 including the Lobby wait and Day phase (tickBurn had no phase awareness, defaulted to Night 1's rate at boot) -- confirmed live via the user's own console log showing Night 1 opening directly into a Blackout. Fixed with a new Config.HEARTH_DAY_BURN_RATE and CycleManager.enterDay() re-applying it every Day phase. (3) Pressing F to toggle the hand lantern did nothing visible -- the architecture called for a dedicated LanternController LocalScript that was simply never built; server-side tracking worked the whole time but there was zero PointLight/visual representation. Built LanternController (PointLight for On, a 35-degree SpotLight cone for Focused Beam, both riding a Head Attachment so they aim wherever the character faces with no per-frame code). All three were code-review-invisible -- they only surfaced because a real person actually played the game. Also added HearthManager.ResetForRun() (Tier 1, full fuel) wired into CycleManager.EndRun(), a related but secondary gap.
- Started the visual polish pipeline in response to "the world looks very blocky and outdated" -- expected at this stage, since no detail/prop/lighting pass had run yet, only bare world-builder primitives. Launched in parallel: a full paranoid code review (luau-reviewer) and the first polish stage, architectural detail (detail-architect).
- Architectural detail pass complete and independently verified: 281 new parts across all 8 zones + Waxworks' floor (root-flare systems on ~15-20% of trees per zone computed live from each tree's actual trunk CFrame, not guessed coordinates; worn-dirt path patches following real desire lines between the Lodge/CampSpawn/ArrivalPoints/trail exits; moss/lichen overlays on rocks; ground clutter; Lodge trim/baseboards/fascia; wax drip detail at the Waxworks' 18 columns, the one deliberate Tallow White usage, correctly confined to that zone only). Verified: 0 unanchored, 0 CanCollide=true (pure visual layer, doesn't obstruct anything), 0 Tallow White outside Waxworks, 0 SpecialMesh Pyramid/Prism, 0 trails narrowed (raycast-checked), part-count math reconciled exactly (236 in named ArchDetail folders + 41 in CampClearing's existing subfolders + 4 in Waxworks.Floor = 281). Agent caught and cleanly fixed its own mid-process duplication bug (a retry after an invalid Enum.Material.Moss error had re-run and doubled North Pinewood's detail) before it ever reached this report.
- Next in the polish pipeline: set-dressing (decorative props per zone, parallel), then sound/VFX, then cinematic lighting (post-processing -- likely the single biggest lever left for "looks outdated," since zero Bloom/ColorCorrection/Atmosphere/DepthOfField exists yet), then an art-director composition review.
- FULL CODEBASE REVIEW completed (luau-reviewer, all 22 scripts / ~6,300 lines): 3 Critical, 21 Serious, 14 Moderate, 9 Low findings. Full detail in gamemaster/code_review_round1.md. Headline: harvesting had NEVER been reachable by real player input this entire session (nothing anywhere fired RequestHarvest -- InputController only bound E to hearth-feeding; every earlier "verified" harvest test was via direct script injection), and the Downed state was a terminal, unrevivable, non-punishing dead end (no revive handler, no elimination path, no wipe condition, and Downed players moved at full speed) -- meaning getting grappled by Wick was accidentally a buff, not a threat. Also found: Gloom's death-drain damage was 10x too fast (30 HP/s instead of 3/s, not dt-scaled against a 10Hz tick).
- Launched 5 parallel fix agents split cleanly by file ownership (no two agents touching the same file) to work through the review's recommended fix order. Two rate-limit interruptions occurred mid-batch; resumed cleanly, and in both cases most of the interrupted agent's work had already landed correctly before it died (verified via compile checks + grep before relaunching only the missing remainder, avoiding duplicate work).
- CRITICAL FIXES CONFIRMED LANDED AND VERIFIED (compile-clean, grep-confirmed): (1) Harvesting is now genuinely reachable -- E is context-sensitive (nearest HarvestNode vs Hearth, whichever is closer), fires real RequestHarvest calls, this is the single most important fix of the whole session since it unblocks the entire economy for the first time. (2) Downed is now a real, temporary, consequential state -- SurvivalStats has a working RequestRevive handler (range + cooldown gated), a 45s bleed-out timer that transitions to Snuffed, a genuine wipe condition (CycleManager.EndRun("wiped") when zero players are Alive -- previously EVERY run ended in "survived" since there was no other path to EndRun), and Downed players are now correctly immobilized (WalkSpeed 0) instead of moving at full speed. (3) Gloom death-drain is now correctly dt-scaled (~3 HP/s as intended, not 30).
- SERIOUS FIXES CONFIRMED LANDED: Hearth no longer burns fuel during the Lobby wait (proper phase gate, not just the earlier rate-only fix) and burn rate resets between runs; Hearth Tier 2/3 upgrades now correctly widen the lit radius (was hardcoded to Tier 1 regardless of actual tier, making the safe zone bigger than the lit radius after an upgrade); RequestHearthUpgrade validates before spending instead of after (previously a bad tier request destroyed resources for nothing); destroyed lights (Placed Lanterns/Brands cleared at run end) now correctly unregister instead of permanently ghost-illuminating their old position; hand-lantern fuel drain no longer spams InventoryUpdated at ~60Hz (throttled to fire only when the displayed value changes, plus the underlying tick moved to a 0.25s accumulator); CraftingManager's per-run gates (WatchtowerBeacon/StoneAxe one-time flags, CoCraft sessions) now actually reset between runs, and CoCraft no longer silently reassigns the payer if the initiator disconnects mid-session; player inventories now reset between runs; harvesting now costs stamina and has a real per-node cooldown (was previously free, instant, and spammable); RequestCraft/RequestHarvest reject while still in the Lobby; Watchtower/Cellar occupancy no longer flickers from per-limb Touch/TouchEnded (same bug class as the already-fixed Ready Pads, now fully fixed everywhere it occurred); WickAI's presence-stream loop (a while+task.wait poll that ran forever even when Wick was despawned) is now a proper Heartbeat accumulator, net removing a top-level local rather than adding one (register-budget-safe); Mark Wick now broadcasts Wick's actual position instead of the marking player's own position (was completely backwards) and costs Gloom as intended; the dead unreachable Melting *state* was cleanly removed (the working Melt visual overlay is untouched); decision-timer drift (~4% per tick) fixed by capturing real elapsed time instead of the nominal interval; lantern-mode desync across InputController/LanternController/WickAI fixed (all three now correctly reset on respawn and fuel exhaustion); game:BindToClose added so DataStore saves aren't lost on shutdown.
- StructureManager's RequestInteract handler (S4) and Lantern Post relighting (S21) confirmed already complete from the original interrupted agent's work -- a follow-up verification agent found it fully correct (rate-limited, validated, range-checked, spends 2 Tallow via ResourceManager, calls the already-working LightManager.Relight) and made zero changes rather than risk double-registering the handler. My own grep checks had twice missed it due to a local-variable-name mismatch (`requestInteractRE`, not the literal string I searched for) -- resolved by reading the file directly, which confirmed the handler is real and correct.
- **ALL 5 CODE REVIEW FIX BATCHES NOW COMPLETE AND VERIFIED**: every Critical and Serious finding from the round-1 review is fixed, compiling clean, and cross-checked against real dependency signatures. Moderate/Low findings remain as documented follow-ups (see code_review_round1.md) but nothing game-breaking remains open from this pass.
- SET-DRESSING COMPLETE across all 8 zones, 6 parallel agents, all independently verified: Camp Clearing + Lobby (150 props -- the "left in a hurry, mid-ordinary-life" ranger-camp story: bedrolls, a still-hanging kettle, laundry on the line, a corkboard, tools), North Pinewood (61 -- a cold campsite and an axe still lodged in a stump), East Boulder Field (43, deliberately sparse per its own design), South Lakeshore (70 -- a tied-off rowboat, a dock stub, driftwood), West Deadfall (70 -- bone-dry brush, bones, spiderwebs reinforcing the zone's hiding-spot design), The Waxworks + Trail Ring (51 -- wax reclaiming an abandoned cache, supplementary trail cairns). 445 new props total, every zone independently confirmed: 0 unanchored, 0 CanCollide=true, 0 SpecialMesh Pyramid/Prism, 0 Tallow White outside the Waxworks, 0 harvest-node/trail/concealment-volume/sightline violations. Two agents caught and fixed their own mid-process issues before reporting (a duplication bug in Camp Clearing, a harvest-node clearance violation in South Lakeshore, a cylinder-orientation audit in Camp Clearing).
- Starting cinematic lighting (lighting-director): currently zero post-processing exists anywhere (no Bloom, ColorCorrection, Atmosphere, or DepthOfField) -- likely the single biggest remaining lever for the "looks outdated" complaint, since the architecture's own detailed Lighting Design Brief specifies a full day/night post-processing stack that hasn't been built yet.
- CINEMATIC LIGHTING COMPLETE and independently verified: full Day/Night post-processing stack (ColorCorrection, Bloom, DepthOfField, SunRays, Atmosphere) now tweens on the same Dusk(15s)/Dawn(12s) beat as the existing color/brightness shift, cleanly extended into CycleManager's already-tested tween logic (verified via string-diff that phase control flow is unchanged) rather than replacing it. Atmosphere Density now correctly escalates per-night (0.12/0.16/0.20/0.24/0.26) sourced from Config.NIGHTS, hard-clamped at the 0.26 safety ceiling. Bonus: low-fuel flicker added to LightManager (any light below 20% fuel jitters brightness +-35%), cleanly extending the existing fuel-tick Heartbeat with zero new connections.
- Agent found and fixed two real pre-existing problems while building this: Atmosphere.Density was ALREADY at 0.30 -- over the architecture's own 0.26 hard safety ceiling (whiteout risk on mobile) -- and DepthOfFieldEffect was sitting at stock Roblox defaults with Enabled=false and a gameplay-breaking near-blur value. Both corrected as part of this pass.
- MANUAL STEP NEEDED FROM THE USER: Lighting.Technology cannot be read OR written by any script in this MCP session (a plugin-level restriction, not the usual require()/FireServer() one) -- confirmed independently. It must be set to Future manually in Studio (select Lighting in the Explorer -> Properties -> Technology -> Future) for the ~15 dynamic PointLights in the world (Hearth, Lantern Posts, Wick's candle, crafted lanterns) to actually cast shadows -- currently still on the file's original ShadowMap setting, which is the one piece of "one warm fire in a cold blue forest" that automation cannot finish.
- Two follow-up design gaps flagged (not yet acted on): (1) Hearth's visual light Range never scales with Tier upgrades (stays at 60 studs even at T3, though Config specs up to 95) -- nothing in HearthManager writes to it. (2) A significant player-facing perception trap: the Hearth's VISUAL light reaches 60 studs but the GAMEPLAY "am I lit" radius (which drives Gloom and Wick detection) is only Config.LIGHTS.Hearth.range=24 studs at T1 -- a player standing 40 studs out looks bathed in light but is mechanically in the dark. Worth a deliberate balance decision (pull the visual radius down to match, or make the true safe radius legible some other way) rather than an automated fix.
- User asked to build the full game before their own test pass. Launched sound-designer + vfx-designer in parallel (the game had zero audio and zero particle effects up to this point).
- VFX PASS COMPLETE and independently verified: 15 ParticleEmitters (combined rate 55/80 budget), covering embers+dust at Camp, needle-drift+ground-fog+canopy-dust at Pinewood, minimal grit-dust at Boulder Field (deliberately sparse per its own openness-is-the-point design), shoreline-fog+lake-mist at Lakeshore (the lake surface itself confirmed completely untouched -- no ripple VFX, preserving a planned signature moment), heavy ground-fog at Deadfall (the most oppressive zone), wax-drip+cache-haze at the Waxworks. Zero VFX on Wick's rig (confirmed) and zero on Trail Ring/Boundary, both intentional. Agent deliberately left 5 emitters/25 particles-per-sec of budget headroom for two effects that need script-triggering rather than static placement: Wick's melt-stage wax drip (needs WickAI.GetMeltStage()) and day/night fog toggling (the 5 fog/mist emitters are architecturally Night-only but currently always-on at a subtle rate, pending a future scripter pass) -- both clearly flagged as follow-ups, not silently skipped.
- SOUND PASS COMPLETE and independently verified: 18 real Sound objects (18/25 budget) -- the Hearth fire is deliberately the loudest, warmest sound in the game (0.45) so leaving Camp is an audible loss; sparse cold ambient beds in every outdoor zone scaled to actual zone footprint (West Deadfall intentionally near-inaudible at 0.09, matching its "no animal audio at all" design -- flagged clearly as deliberate, not a bug); 10 spatial point sources (Old Mother groaning, the collapsed fire tower, the rowboat hull, wax dripping); Wick's own hum placed on its rig, correctly Playing=false by default pending state-driven control. Agent went beyond the brief and actually searched for and load-tested real audio assets (AssetService:SearchAudio + ContentProvider:PreloadAsync, confirmed IsLoaded/TimeLength) rather than placeholder SoundIds -- zero placeholders. Verified: 0 volume violations, 0 empty SoundIds, 3 SoundGroups present, Wick_Hum correctly silent with the right attributes (WickHum=true, BaseVolume=0.45) for a future scripter to wire against WickAI.GetState()/the existing WickPresenceStream humIntensity value -- flagged as the single highest-value remaining hookup, since the hum is the game's core identity and currently never plays. Day/Night-only sounds (birds, night insects) tagged with discoverable DayOnly/NightOnly attributes for the same future muting pass VFX's fog needs.
- World is now fully built, detailed, dressed, lit, and scored: 8 zones, ~2,220+ parts, 445 props, full cinematic lighting, 15 VFX emitters, 18 sounds. Moving to the composition review (art-director) now that every visual+audio layer is in place.
- COMPOSITION REVIEW COMPLETE: COMPOSITION VERDICT NEEDS DIRECTION, but the headline is good news -- the game's core 5-color palette contract held perfectly (0 Tallow White violations across 2,316 parts) and the critical "can't tell a teammate's lantern from Wick's antlers" inversion measurably works (identical RGB, near-identical falloff). The issues are all in the human-built layer, which drifted into an unowned 6th color (warm brown) instead of using the designated Ashen Birch. 19 numbered findings total, tagged by responsible agent. Full detail in the session record.
- Fixed 5 findings directly (quick, low-risk property changes, self-verified before and after): Waxworks' 3 candle flames were off-model amber (255,196,120 vs the game's real 224,138,60 used everywhere else -- the one place amber ambiguity matters most); the saved Lighting was stale (ClockTime 14.5/Brightness 3/flat grey Ambient instead of the real Day preset -- meant the Lobby, the very first thing every player sees, rendered off-palette and 2x too bright); the 4 Lobby lights taught players the wrong "safe color" (224,168,96 instead of 224,138,60); a redundant default-Studio Baseplate with its grid texture was exposed past the tree boundary (SafetyGround terrain already fully covers the play area, confirmed before deleting); the lake's sandy bed was bleeding a warm brown tone through the cold lake surface.
- Launched 3 more fix agents for the review's remaining highest-leverage findings: script-wiring day/night audio+VFX toggling (birds currently sing at night, fog runs during the day) plus Hearth-tier-scaled light radius (the light currently promises 60 studs of safety when the real mechanical radius is 24-46 depending on tier -- flagged as "the light lying to the player", the worst kind of composition problem in a game whose whole safety language is color-coded); decluttering the Waxworks (a set-dresser pass over-filled it to 37+ floor items against a ~40-prop sparse-cathedral budget, erasing the negative space that makes the column colonnade read as a colonnade); and subtle unlit moonbreak lighting (Moonshade Blue, no shadows, explicitly NOT amber) on the 4 zones' landmark focal points that currently have zero illumination at Night -- carefully scoped to not contradict the architecture's original deliberate "no fixed lighting" design for those zones.
- Remaining lower-priority composition findings, documented as follow-ups for a future round: East Boulder Field's Stone Shelf focal point is shorter than the boulders around it; West Deadfall's collapsed fire tower's best silhouette lies flat inside the ground fog; trail marker paint and bone-prop colors sit too close to the reserved Tallow White; 17 near-identical brown shades on the Fire Tower read as mush rather than weathering; 3 zone-landmark sounds share one identical audio file; the Trail Ring has zero atmosphere (no sound/VFX/light at all).
- Day/night audio+VFX wiring and Hearth-tier light scaling both landed and verified: CycleManager now flips 3 phase-specific sounds and 5 fog emitters on real Day/Night transitions (birds no longer sing at night); HearthManager now syncs the Hearth's visual PointLight Range/Brightness to the actual tier at Upgrade/ResetForRun/Init, so the light no longer promises more safety than the mechanics deliver.
- Waxworks decluttered and verified: wax accents cut from 37+ evenly-scattered floor items down to 26, consolidated into 3 deliberate pools (matching the 4 columns the VFX pass had already picked for particle drips) with genuine open floor (15-21 stud clearances) between them. All protected structures (18 columns, Floor, Candles, Ranger's Cache, WickDen, WickRestPoint, VFX) confirmed untouched.
- MID-SESSION STUDIO CRASH: lost the first moonbreak-lighting attempt (it passed its own verification but was never saved before Studio crashed and had to be restarted). Re-ran the identical task from scratch -- all 4 lights (Moonbreak_OldMother/StoneShelf/Rowboat/FireTower) confirmed present this time with correct values. IMPORTANT LESSON for this project going forward: changes only exist in the live Edit-mode DataModel until the user manually saves (Ctrl+S) -- asked the user to save now and periodically, since a lot of unsaved work has accumulated across a long session.
- ALL composition-review highest-leverage fixes now complete and verified: candle color, stale Lighting snap, Lobby light color, redundant Baseplate removal, lakebed tint (all direct), day/night audio+VFX wiring, Hearth-tier light scaling, Waxworks declutter, and 4-zone moonbreak lighting (all via agents, all independently verified).
- UI VISUAL POLISH COMPLETE and independently verified: SurvivalHUD + CraftingGui restyled onto the game's real 5-color system (71 elements -- 30 UICorner/25 UIStroke/26 UIGradient/12 UIPadding/9 UISizeConstraint), SpecialElite+RobotoMono fonts replacing default SourceSans, a proper radial Gloom vignette (was a flat tint), all Offset-based sizing converted to Scale for mobile. Correctly preserved the Gloom bar's Tallow White fill as an intentional, sole exception to the color-reservation rule (confirmed: exactly 1 hit game-wide, verified independently). Good judgment calls: recognized a generic hover-animation script would have clobbered CraftingUIController's existing affordability-tint logic and built a UIScale-only animation instead; found `Cartouche` doesn't exist as a Roblox font and substituted appropriately. All 55 WaitForChild/FindFirstChild paths the two existing (untouched) controller scripts depend on verified still resolving correctly -- zero logic properties touched. Flagged one remaining off-theme element (a runtime-constructed WickPositionLabel inside HUDController itself, out of a pure-visual agent's remit to fix).
- Starting final structural QA pass (roblox-playtester) -- the last gate before handing back for a full playtest.
- STRUCTURAL QA COMPLETE: VERDICT NEEDS FIXES, but the build is otherwise excellent (22/22 scripts strict+compiling, zero deprecated API, zero unanchored parts, flawless Wick rig, 150/150 valid harvest nodes with zero duplicate IDs, every UI path resolving, every performance budget met). The failure is entirely in the client<->server input surface, and it's a precise, important finding: the earlier code-review fix pass built correct, fully-validated SERVER handlers for RequestRevive/RequestHearthUpgrade/RequestMarkWick -- but never added a CLIENT trigger for any of them. Mirror-image on the way back: 12 of 18 server->client broadcasts (including BlackoutTriggered, RunEnded, DawnReport, PlayerStateChanged, and all 8 of WickEvent's Signature-Moment beats) have zero listeners. Also found: RequestInteract's only client fire site sends ("Hearth","Feed") but the server handler only accepts "Relight" -- so Lantern Post relighting (the main light-recovery mechanic) has been completely unreachable this whole time despite the underlying LightManager.Relight logic being correct. And Wick's hum Sound (verified correctly configured, Playing=false) is never told to Play() by anything -- the monster has been completely silent.
- CRITICAL CORRECTION to earlier session claims: the "Downed/Revive/Wipe is a real working loop" milestone reported after the code-review fix pass was only half true -- the server-side logic is genuinely correct (verified again), but no player could ever actually trigger a revive, since InputController never fires RequestRevive. This is exactly the kind of gap the QA pass exists to catch, and it caught it.
- Launched 2 parallel fix agents (split by file ownership): InputController+WickAI (add revive/mark-wick client firers with a Downed-teammate priority check, fix the RequestInteract payload so Lantern Post relighting actually works, make Wick's hum Sound actually play on spawn) and HUDController+CraftingGui/CraftingUIController (listen for PlayerStateChanged/BlackoutTriggered/RunEnded/DawnReport via the existing toast system, add the missing Hearth Tier 2/3 upgrade UI).
- Lower-priority QA findings deferred as documented follow-ups: no mobile touch-input layer at all (doesn't block a desktop playtest); RequestPlaceObject/RequestThrow fully dead (Brand/Flare throwing unimplemented, though crafting itself works via auto-placement); WaterVolume tag never read (wading has no speed/noise penalty yet); up to ~27 concurrent lights against an ~18 mobile budget (desktop-safe, worth revisiting for mobile); several documentation-only drifts between architecture.md and the built game.
- QA FIX BATCH COMPLETE and independently verified (both agents, all claims cross-checked via compile + grep): InputController now fires RequestRevive (priority-1 in the E-hold action, ahead of harvest/feed) with a client-side Downed-teammate tracker fed by a new PlayerStateChanged listener; RequestInteract's payload bug fixed (was always sending the dead "Hearth"/"Feed" pair -- now correctly targets a nearby snuffed LanternPost with "Relight" as priority-3, meaning Lantern Post relighting is finally reachable); G now fires RequestMarkWick with a client debounce matching the server's real 8s cooldown; Wick's hum Sound now actually Play()s on spawn -- the monster is no longer silent. HUDController gained listeners for PlayerStateChanged (a new DownedOverlay + toast), BlackoutTriggered, RunEnded, and DawnReport, all routed through the existing toast queue rather than a new ad-hoc UI path. CraftingGui gained 2 new rows (Hearth Tier 2/3) wired to the previously-unreachable RequestHearthUpgrade, gated on Hearth proximity the same way Workbench recipes gate on Workbench proximity.
- User asked to finish everything before their own test pass, and separately asked whether better (non-primitive) 3D models could be built -- discovered this Studio session now has AI mesh-generation tools (generate_mesh/generate_texture/segment_mesh) that weren't available earlier. Launched 2 parallel model-upgrade agents (enemy-designer for Wick's rig, world-builder for the ~152 primitive-cylinder trees), both with exhaustive contracts to preserve every load-bearing property (Wick's exact R6 part names/Motor6D C0-C1/HipHeight/PrimaryPart; the 70 HarvestNode-tagged trees' tags/attributes/positions). Both died instantly to a weekly (not session) rate limit before making any changes; relaunched identically once the studio reconnected on a fresh id and the weekly window should have reset.
- WICK'S VISUAL MODEL UPGRADED and independently verified: all 6 non-root R6 body parts (Head/Torso/L+R Arm/L+R Leg) replaced with real AI-generated meshes (elongated wax-deer skull, ribbed drip-pooled torso column, spindly clawed bone arms, digitigrade cloven-hoof legs) via generate_mesh, up from primitive blocks. Built with real engineering discipline: generated each part individually (segmentation="none") rather than one segmented creature after a first segmented attempt came back geometrically degenerate (overlapping arm/leg meshes, a sideways torso); moved the actual Motor6D Instances across from the old parts rather than recreating them, so C0/C1 values were never retyped and cannot have drifted; forced CollisionFidelity=Box so collision matches the original primitive bounds exactly; did all work on a clone (Wick_NEW) before promoting, keeping the full original rig as Wick_BACKUP_Primitive in ServerStorage. Independently re-verified via a fresh MCP read (not just trusting the agent's self-report): all 7 part names present and correctly typed, all 6 Motor6D C0/Part0/Part1 values byte-identical to the documented pre-upgrade baseline, HipHeight=1.8/WalkSpeed=9/MaxHealth=100/PrimaryPart=HumanoidRootPart all unchanged, CandleTip_Center's PointLight fully intact (Range16/Brightness1.3/#E08A3C/Shadows true), total part count still 15, WickAI.lua compiles clean with zero edits required. Also confirmed non-obvious contracts were protected: the rig's lowest bounding-box point (used by WickAI's spawn foot-placement) is byte-identical (delta 0.000000), and the new meshes use TextureID only with no SurfaceAppearance (which would have silently broken the Melt-stage Transparency fade). Deliberately left the antler tines/candles untouched after 3 rejected mesh attempts (base-barrel geometry, a full second skull, a literal candelabra) -- correctly judged that risking the load-bearing candle weld graph for a cosmetic upgrade wasn't worth it, and proposed a safer follow-up (a standalone antler prop iterated separately, then welded on).
- TREE MODELS UPGRADED and independently verified: all ~179 trees across North Pinewood, East Boulder Field, South Lakeshore, West Deadfall's snags, and the Boundary ring now use real AI-generated conifer/dead-tree meshes (generate_mesh) instead of primitive cylinder stacks -- 4 reusable templates (PineA/PineB/DeadA/DeadB, stored at ServerStorage.TreeMeshTemplates) cloned with per-tree scale/yaw/tint variation, not 179 individual generations. The critical constraint held perfectly: all 70 North Pinewood HarvestNode-tagged Wood trunks are byte-identical in Name/Position/CFrame/Size.X/tags/attributes/Anchored/CanCollide (max position drift across all 70: 0.00007 studs), because the agent kept each original tagged trunk cylinder as the real collider/harvest-target and made it invisible by occlusion (slimmed + bark-colored to hide inside the new mesh) rather than risk moving the tag onto a MeshPart whose bounding-box-center Position would have broken the 12-stud harvest range. Untagged zones got the cleaner treatment (genuinely Transparency=1 or deleted trunks). Verified: 150/150 HarvestNodes total unchanged, 70 unique Wood NodeIds zero duplicates, 0 SpecialMesh Pyramid/Prism anywhere, tree-zone part count 664->360 (-304), all 37 non-tree folders confirmed descendant-identical before/after. Deliberately left as follow-ups: Old Mother (still the original primitive stack, now visibly dated next to 70 detailed conifers -- a bespoke mesh at its actual 36-stud-trunk/50-stud-canopy scale would suit it) and West Deadfall's 6 Concealment-tagged Hollow Stumps (a StumpD template exists in the library but those carry gameplay tags and need the same careful trunk-preservation treatment); 21 FallenLog models correctly left as primitives (horizontal logs, not standing trees, wrong shape for any generated template).
- BRAND/FLARE THROWING IMPLEMENTED (buglist #10, previously a fully dead RemoteEvent pair) and independently verified: a new ServerScriptService.Systems.ThrowManager handles RequestThrow end-to-end (rate limit -> run-started gate -> itemId whitelist -> unit-vector validation -> ownership/spend via new ResourceManager.ConsumeThrowable), spawns a real physical projectile (Cylinder for Brand, Ball for Flare, its own Players<->Projectile collision group so it passes through players per the architecture's Physics table), launches it with AssemblyLinearVelocity, and resolves each tick to a Wick hit (WickAI.DamageWax -- Brand 15/Flare 25 Wax, routed through the exact same addWax() clamp/melt-evaluation path the night burn-tick already uses, not a duplicate), a water hit (OBB test against the WaterVolume tag, instant destroy -- the "hiss" SFX was deliberately left unwired since no matching one-shot sound asset exists, only an unrelated looping candle ambience), or a 2s-timeout anchored landing. Players acquire a throwable by crafting Brand/SignalFlare as before -- crafting now ALSO grants one carried throwable via ResourceManager.GrantThrowable, purely additive lines appended after the existing (byte-for-byte unchanged, independently diffed) registerPlacedLight() calls, so the already-shipped placed-fixture behavior is untouched. RequestPlaceObject was deliberately left dead -- nothing in the built game places an object at an arbitrary player-chosen position, every prefab auto-places at the crafting spot, so there's no real use case for it yet. Independently re-verified (not just trusting the agent): script_grep confirms exactly one RequestThrow server handler and exactly one real client FireServer call with matching (itemId, direction) payloads; WickAI.GetWickPosition/DamageWax both exist as function-sugar (no new top-level local, confirmed via a fresh loadstring compile-check that would have failed at 200+ registers); all 8 touched/created scripts (ThrowManager, WickAI, ResourceManager, CraftingManager, Main, Config, InputController, StructureManager) compile clean via loadstring; StructureManager's Relight handler confirmed byte-for-byte unchanged. T is the new client throw key (Brand preferred over Flare if both carried), firing with the player's real LookVector.
- TRAILRING AMBIENT AUDIO ADDED and independently verified: the only zone in the game with zero Sound objects (flagged in the final structural QA pass) now has 4 -- two split-arc ambient beds (same asset, different PlaybackSpeed north/south so the ring doesn't phase-lock with itself) plus 2 spatial point sources (a swaying abandoned-lantern creak, a groaning trail-marker post) placed at the ring's two loneliest nodes. Deliberately avoided wind (Boulder Field's exclusive signature per architecture) and avoided any bird-bearing asset (birds vanishing at Dusk is the game's best "something is wrong" cue elsewhere -- a permanent bird bed on the connective trail would have undercut it everywhere). Game-wide sound count 19->23/25. Verified independently: all 4 SoundIds real and confirmed loaded, all Playing=true, volumes 0.13-0.16 (well under the 0.7 cap), correct SoundGroup assignment, zero other zones touched.
  - The agent also flagged what it read as a pre-existing bug: `Spatial_Lakeshore_NightInsects` "permanently playing" against the architecture's "crickets during DAY, absolute insect silence during NIGHT" line. Investigated directly -- this was a FALSE ALARM caused by a static snapshot read (no live Play-mode testing is possible this session, so "permanently playing" just meant "Playing=true at the moment it was checked", which is correct for a `NightOnly` sound checked during Night). The sound already carries a correct `NightOnly=true` attribute and is properly gated by `CycleManager.applyDayNightAudioVisuals`. I nearly made this worse by adding a conflicting `DayOnly=true` on top before catching my own error -- reverted immediately, confirmed back to the original correct state. No actual bug here; logged so no future pass re-investigates it.
- WADE RIPPLE VFX ADDED (buglist follow-up item, the wade mechanic's missing visual flourish) and independently verified: a new client-only `WadeRippleController` LocalScript mirrors SurvivalStats' own water-detection logic (same OBB test, zero server changes, zero new RemoteEvents -- SurvivalStats.lua confirmed untouched) and spawns small expanding ripple rings at each wading player's position, visible to everyone (not just the wader themselves, which matters since the water revealing people is part of the zone's design). Caught two real bugs before they shipped: the lake's visible surface (Y=-0.30) sits below the WaterVolume's actual top (Y=+0.50, deliberately overshot so the penalty fires in the shallows) -- ripples anchored to the volume itself would have floated 0.8 studs above the water; and with MaxPlayers=60, a naive one-emitter-per-wader design could have spawned up to 60 emitters (~12x the budget) -- hard-capped at 4 concurrent anchors with nearest-first culling and idle-slot reclamation. Static placed-scene emitter budget unchanged (15/55 p/sec, confirmed byte-for-byte); worst-case runtime peaks at 19/20 emitters and 75/80 p/sec (4 simultaneous waders) -- within budget. Explicitly gated on movement, not just water-presence, so a motionless player in the lake lets it go still (protects the separate reflection signature moment). Verified independently: compiles clean, zero ParticleEmitter/Beam/Trail objects on the Lake itself (ambient stillness confirmed intact), WaterVolume's SpeedMultiplier/StaminaMultiplier/NoiseRate attributes unchanged.
- LOBBYGUI BUILT (buglist #15/#19, the last major missing client UI piece) and independently verified: a new LobbyGui ScreenGui (DisplayOrder=15, matching SurvivalHUD/CraftingGui's exact visual language -- SpecialElite title font, RobotoMono body, amber #E08A3C accent, same UICorner/UIStroke/UIGradient treatment) shows a live per-player ready roster and a "X/Y ready" status, driven by a new LobbyController LocalScript. Zero server changes were needed -- SessionManager already fires LobbyUpdated with the full {userId,name,ready} roster every ready-pad touch, confirmed byte-identical before/after. Correctly discovered and handled a spec-vs-reality gap: architecture.md describes a 60s/15s/5s countdown that SessionManager doesn't actually implement (it starts the instant everyone's ready) -- rather than fake a countdown the server doesn't drive, the UI shows the real ready-count as primary status and leaves the countdown display wired-but-hidden, so it activates automatically with zero client changes if a real countdown is ever added server-side. Hides/reappears correctly by hooking the same CycleChanged phase signal HUDController already uses (Lobby phase = visible, anything else = hidden), which also handles the return trip after a run ends without extra wiring. Verified independently: LobbyGui exists at DisplayOrder=15, LobbyController compiles clean with all 3 real OnClientEvent listeners (LobbyUpdated/RunStarted/CycleChanged) confirmed via grep, zero elements use Offset-based sizing (fully mobile-safe), SessionManager's fire-call confirmed unchanged.
- OLD MOTHER + WEST DEADFALL HOLLOW STUMPS UPGRADED and independently verified: Old Mother (North Pinewood's landmark tree, the last visually-dated primitive left after the earlier tree-mesh batch) replaced with a bespoke 54-stud ancient-sentinel mesh -- massive gnarled trunk, root buttresses, sprawling crooked canopy -- built at the architecture's actual "58-stud unchoppable pine" scale rather than the regular tree templates. Kept the original trunk cylinder as an invisible collision core (unchanged position/height, widened to fit the new visual trunk) and correctly reparented/preserved both zone Sounds (the 25-stud groan trigger's exact position untouched). All 6 West Deadfall Hollow Stumps also upgraded to real hollow-ring meshes with a genuine 150-degree open C-shaped entrance matching the architecture's design intent -- the agent's first generation attempt came back solid and was correctly caught and discarded via its own probe-testing (raycasting the interior from 12 directions) before trying again. Critically, discovered the Concealment mechanic doesn't actually depend on the visual stump walls at all -- it's a separate invisible `_Volume` part per stump that WickAI checks via a pure OBB point-in-box test -- so the visual swap carried zero mechanical risk once that was understood; all 6 volumes confirmed byte-identical (position/size/attributes) and all 18 original collision walls preserved exactly (only Transparency changed 0->1). Independently re-verified: HarvestNode still 150, Concealment tag still 6, Old Mother has a real MeshPart with the trunk collider intact, Lighting confirmed byte-correct against CycleManager's DAY_LIGHTING preset after the agent's own mid-task lighting-preview mistake (briefly zeroing Atmosphere.Density to attempt a screenshot) was caught and self-corrected. Net part count: North Pinewood -3, West Deadfall +6.
- WICK'S ANTLERS: CORRECTLY SKIPPED after exhaustive good-faith attempts. Ran 7 more `generate_mesh` iterations (on top of the original agent's 3), rejecting every one on structural grounds, not taste -- verified via a genuinely clever method after screen_capture returned a stale/broken frame: read each candidate mesh's real geometry through AssetService:CreateEditableMeshAsync and projected it to ASCII occupancy grids, rather than guessing from an unreliable screenshot. The decisive finding: Wick's 3 candle tips sit at 3 fixed, deliberately asymmetric points relative to Head (spanning 5.63 studs, center candle highest) that the load-bearing CandleTip_Center PointLight depends on -- every generated antler rack came back bilaterally symmetric, meaning any real swap would leave the most important candle floating over empty air. The existing thin-cylinder tines hit those exact points because they were hand-authored to; no generated mesh could without either moving the light (forbidden) or looking worse than the primitives it would replace. Correctly concluded a cosmetic upgrade isn't worth that trade on the game's most complex system, and left Wick completely untouched -- independently re-verified: still exactly 15 parts, CandleLight still present and correctly parented, ServerStorage contains exactly the 3 expected children (Wick/Wick_BACKUP_Primitive/TreeMeshTemplates) with zero leftover test debris from either this or the interrupted prior attempt. Left a concrete, sound follow-up recommendation for later: hand-author the rack from primitives (subdivide/branch the existing tines rather than generate a replacement), which can preserve the exact 3 candle anchor points at a cost of ~10-15 extra parts -- a job for a scripter/builder doing manual primitive work, not mesh generation.
- FINAL POST-BATCH STRUCTURAL QA COMPLETE: VERDICT NEEDS FIXES, but the entire concurrent batch itself (Wick model upgrade, tree model upgrade, wade mechanic, light trim, Brand/Flare throwing, round-2 review fixes -- all landed together, two of them touching WickAI.lua concurrently) was independently re-verified with ZERO regressions: all 23 scripts compile, Wick's rig contract re-derived from scratch and confirmed byte-identical to the primitive backup, all 150 HarvestNodes (70 unique Wood NodeIds) survived the tree-mesh swap untouched, RequestThrow's full client-server path verified end to end, part count down to 2015 (-342 from the batch), light trim confirmed (8 shadow-casters -> 1). The 2 real issues found were BOTH pre-existing and unrelated to the batch: Workspace.StreamingEnabled=true (architecture mandates false -- causes lights to pop in/out at distance, a direct gameplay bug in a game about reading light from afar) and a malformed state.json (a missing comma I introduced in my own edits earlier this session). Also flagged: a fully-built, fully-wired Cellar concealment subsystem was completely unreachable (CellarHatch was CanCollide=true with zero script ever reading its Open/Locked attributes) -- one of only two concealment options in camp, dead since Phase 2. Fixed all 3 immediately: Workspace.StreamingEnabled set to false; state.json's JSON syntax corrected and validated; CellarHatch (and its already-non-collidable handle prop) set CanCollide=false as a "trapdoor" abstraction -- visually a closed hatch flush in the floor, but now genuinely passable, confirmed via a fresh raycast/CanCollide check rather than assumed. Lower-priority findings (LobbyGui missing, G/T input-mapping doc drift, TrailRing's zero ambient audio, WickDen's unconsumed AlertRadius, GeographicLatitude doc drift, a dead legacy ResourceNode tag on 114 parts) logged as documented follow-ups, none blocking.
- ROUND-2 CODE REVIEW RE-VERIFICATION COMPLETE (ran concurrently with the Brand/Flare throw agent, both touching WickAI.lua -- independently confirmed zero conflict, both sets of changes merged cleanly, all 8 touched files compile clean). Of the round-1 review's 14 Moderate + 9 Low findings: 6 were already fixed as side effects of earlier work (DataManager schema validation, ShoutPing clock, placePrefab refund-on-failure, decision-timer drift, spammable-harvest stamina cost, all already-shipped); 7 genuinely still open and fixed now -- WaxLevel attribute was replicating at 60Hz unthrottled, now ~6.7Hz via a waxAttrSyncedAt field added to WickAI's existing Melt table (register-safe, always allows immediate sync at 0%/100% boundaries); HUDController's toast queue had a real lost-wakeup bug (an unprotected playToast() error inside the queue's spawned loop would wedge toastBusy=true forever, silently killing all future toasts) -- wrapped in pcall; NoiseManager's noise-meter pings were only pruned as a side effect of GetPings(), which only runs while Wick is actively Seeking/Investigating, so Day/Lobby noise pings sat unpruned indefinitely -- extracted a shared prunePings() now also called from Emit(); phase-gating added to 5 more remotes that lacked it (RequestFeedHearth, RequestHearthUpgrade, RequestInteract, RequestShout, RequestRevive all now reject outside a real run, matching the pattern RequestHarvest/RequestCraft already had); a stale doc comment in ResourceManager still said "ResourceNode" for the tag renamed to HarvestNode early in the project, fixed; WickAI's decisionTick had two byte-identical Day/Lobby and Dawn branches, merged into one condition (register-negative). 2 items re-audited with corrected, more accurate current findings rather than trusting the original count: the "11 unlistened server->client remotes" figure is now genuinely 8 (LightStateChanged/WickEvent/CoCraftPrompt/ShoutPing/WickMarkPing/StructureUpdated/LobbyUpdated/RunStarted), and DawnReport's all-zero payload is confirmed still real (CycleManager.enterDawn fires literal zeros) -- both correctly left as documented follow-ups rather than rushed, since closing either means building multiple new real UI/data-wiring features, not small fixes. 2 items confirmed as needing a human design decision, not a bug: Focused Beam's LookVector-based aim (already documented, buglist #6) and WatchtowerBeacon's occupancy-gating (an existing, deliberate code comment already flags this as a future choice). 1 informational-only finding reported without deleting anything: several genuinely dead Config/Shared/Validator symbols (Shared.Distance/Lerp/Round/FormatTime/FormatPercent, Validator.Boolean, several unbuilt-feature Config.NOISE.actions keys).
- WADE MECHANIC WIRED (buglist #11, previously dead): the South Lakeshore WaterVolume-tagged part had SpeedMultiplier=0.5 and NoiseRate=22 attributes sitting unread since Phase 5, and Config already had wadeMultiplier=1.6 and NOISE.actions.wade=22 pre-authored but never called -- a "partially wired mechanic" of exactly the pattern this whole project's QA passes keep finding. Fixed directly in SurvivalStats.lua (the single existing WalkSpeed/stamina/noise funnel, no new subsystem needed): added an OBB point-in-box findWaterVolume() helper against CollectionService's "WaterVolume" tag; applyWalkSpeed now multiplies by the volume's SpeedMultiplier attribute when a player's HumanoidRootPart is inside it; tickStamina now emits the "wade" noise action once/real-second while wading (unconditional on sprint, matching the architecture's "+22/s" table) and multiplies sprint stamina drain by the volume's StaminaMultiplier while sprinting through water. Also added the missing StaminaMultiplier=1.6 attribute to the WaterVolume part itself -- the architecture's own Tags table specifies all 3 attributes (SpeedMultiplier/NoiseRate/StaminaMultiplier) but only 2 had ever been set. Verified: loadstring compile-clean, attributes confirmed present on the live instance. Ripple VFX on wade (explicitly a vfx-designer job per the architecture, and explicitly NOT ambient/continuous -- "ripples only on player wade, or the trick dies") remains a follow-up, not required for the mechanic itself to function.
- This closes every blocking finding from the structural QA pass. The full input surface this session's earlier fix pass had only half-wired (server-correct, client-silent) is now genuinely complete end to end.
